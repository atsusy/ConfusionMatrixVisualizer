<Window x:Class="ConfusionMatrixVisualizerDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cm="clr-namespace:ConfusionMatrixVisualizer;assembly=ConfusionMatrixVisualizer"
        mc:Ignorable="d"
        Title="MainWindow"
        Loaded="Window_Loaded" d:DesignWidth="366.049" d:DesignHeight="342.079">
    <Grid>
        <cm:ConfusionMatrixVisualizer VerticalAlignment="Center" HorizontalAlignment="Center" CellSize="64"
                                   RowCount="{Binding RowCount}"
                                   ColumnCount="{Binding ColumnCount}"
                                   MatrixValues="{Binding MatrixValues}"/>
    </Grid>
</Window>
